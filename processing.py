# input

# encoder input and output, or just decoder?

# path(s) to files/folders in following formats
    # encoder and decoder:
        # one text file with in and out separated by specifiable value, and pieces of data separated by another specifiable value
        # two text files, one with input, one with output
        # multiple files where each file has one input and output, separated by specifiable value

    # decoder only:
        # one text file 
        # multiple text files each containing one piece of training data

# type of tokenizer

# length of training data